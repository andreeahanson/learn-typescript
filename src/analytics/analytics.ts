console.log('Sending...')

// run in the command line: tsc -init
// so that it runs the changes in any file, not a specified one
// you need to navigate into the right folder!!!!
// after that, you can run only tsc or tsc -w in the command line, and it will look at all files automatically
